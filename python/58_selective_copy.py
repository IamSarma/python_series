# Selective copy
# Program that walks through given folder tree and searched for files
# with given file extension and copy these files from whatever location
# they are into a new folder
import os


def copySpecificFiles(folder):
    return True
